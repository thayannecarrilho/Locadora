<div class="container">
        <h2>Reservas</h2>              
        {{#each cliente}}
        <div class="card">
            <a href="/allreserves/{{this.cpf}}" class="btn btn-dark">{{this.cpf}}</a>
        </div>
        {{/each}}   
    </div>